# yamllint disable
apiVersion: v1
kind: Secret
metadata:
  name: cluster-secrets
  namespace: flux-system
stringData:
  SECRET_DOMAIN: ${BOOTSTRAP_CLOUDFLARE_DOMAIN}
  SECRET_CLOUDFLARE_EMAIL: ${BOOTSTRAP_CLOUDFLARE_EMAIL}
  SECRET_NFS_APPDATA_SERVER: ${NFS_APPDATA_SERVER}
  SECRET_MINIFLUX_ADMIN_PASSWORD: ${MINIFLUX_ADMIN_PASSWORD}
  SECRET_MINIFLUX_POSTGRES_PASSWORD: ${MINIFLUX_POSTGRES_PASSWORD}
